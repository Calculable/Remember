import Foundation

// An anniversary-type of "year" means, that a memory happened 1, 2, 3... years ago. An enniversary-type of "day" means that a memory happened a special number of days ago (for example 1000 days ago)
enum AnniversaryType {
    case day, year
}
